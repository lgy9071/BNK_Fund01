plugins {
    id 'java-library'
}

dependencies {
    // 엔티티에 필요한 표준 애노테이션만 (JPA API)
    api 'jakarta.persistence:jakarta.persistence-api:3.1.0'

    // Bean Validation 애노테이션이 DTO에 있으면
    api 'jakarta.validation:jakarta.validation-api:3.0.2'

}
