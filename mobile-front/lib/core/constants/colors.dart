import 'package:flutter/material.dart';

class AppColors {
  static const Color primaryBlue = Color(0xFF0064FF); // 로그인 버튼 색상 (값 비워둠)

  static const Color fontColor = Color(0xFF383E56); // 폰트 컬러(검은색 대신)

  static const bg = Color(0xFFEFF4FF); // 아주 연한 토스블루(배경)
}